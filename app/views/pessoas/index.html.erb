<section class="content-header">
    <h1>
      <!--Mensagens aqui-->
      <%if notice %>
       <!-- <div class="alert alert-success alert-dismissable">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <h4>  <i class="icon fa fa-check"></i>Mensagem</h4>
          <%#= notice %>
      </div> -->
      <%end%>
    <!--<small></small>-->
    </h1>
</section>

 <!-- Main content -->
 <section class="content">
    <!-- Small boxes (Stat box) -->
       <div class="box box-primary">
             <div class="box-header with-border">
               <h3 class="box-title">Clientes</h3>
              </div><!-- /.box-header -->
      <div class="box-body table-responsive no-padding">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>Nome</th>
                <th>Código</th>
                <th>Sexo</th>
                <th colspan="3"></th>
              </tr>
            </thead>

            <tbody>
              <% @cliente.each do |cliente| %>
                <tr>
                  <td><%= cliente.pessoa.nome_primeiro %></td>
                  <td><%= cliente.pessoa.codigo_cliente %></td>
                  <td><%= cliente.pessoa.sexo %></td>
                <td align='right'>
                  <div class="btn-group">
                      <%= link_to cliente.pessoa, class: "btn btn-default btn-sm", :rel=>"tooltip", :title=>"Ver" do %>
                        <span class="glyphicon glyphicon-eye-open"></span>
                      <% end %>
                     <%= link_to cliente.pessoa, method: :delete, data: { confirm: 'Deseja excluir o registro?' }, class: "btn btn-default btn-sm", :rel=>"tooltip", 'datplacement'=>'top', :title=>"Excluir" do %>
                        <span class="glyphicon glyphicon-trash text-danger"></span>
                      <% end %>
                  </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div><!-- /.box -->
           <%= link_to new_pessoa_path, class: "btn btn-default btn-sm" do %>
                  <span class="glyphicon glyphicon-file"></span>Novo
          <% end %>
  </section>
